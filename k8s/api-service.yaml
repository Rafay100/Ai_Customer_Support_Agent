apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: ai-support
spec:
  selector:
    app: ai-support-api
  ports:
  - port: 80
    targetPort: 8000
    name: http
  type: LoadBalancer
